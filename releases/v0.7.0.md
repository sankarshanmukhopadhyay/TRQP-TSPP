# v0.7.0 — Operational Stack Baseline

This release makes TRQP-TSPP easier to use inside the coordinated TRQP Operational Stack.

## Highlights

- Add shared `run_id` and `target_id` support via the harness CLI.
- Emit posture report metadata required by the TRQP Assurance Hub.
- Add an interop demo profile guide for AL1-oriented demonstrations.
