# GitHub Pages (Jekyll) configuration
title: "TRQP TSPP"
description: "Trust Service Provider Profile documentation and operator guidance for TRQP."
theme: minima
markdown: kramdown
plugins:
  - jekyll-seo-tag
# Ensure these directories/files are published even if Jekyll would otherwise ignore them
include:
  - schemas
  - profiles
  - examples
